# {{ domain.name }} Context Graph — Docker Compose
# Generated by create-context-graph
#
# LadybugDB is embedded — no database container needed.
# This file is provided for running the application services in Docker.

services:
  backend:
    build:
      context: ./backend
      dockerfile: ../Dockerfile.backend
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    environment:
      LADYBUG_DB_PATH: /app/data/ladybug.db
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CORS_ORIGINS: http://localhost:${FRONTEND_PORT:-3000}
    volumes:
      - ladybug_data:/app/data

  frontend:
    build:
      context: ./frontend
      dockerfile: ../Dockerfile.frontend
      args:
        NEXT_PUBLIC_API_URL: http://localhost:${BACKEND_PORT:-8000}/api
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    depends_on:
      - backend

volumes:
  ladybug_data:
