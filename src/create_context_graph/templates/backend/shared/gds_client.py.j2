{% raw %}"""Graph analytics client (stubbed — LadybugDB does not include GDS)."""

from __future__ import annotations


async def check_gds_available() -> bool:
    """GDS is not available in LadybugDB."""
    return False


async def run_community_detection(label: str = "*") -> list[dict]:
    """Community detection is not available in LadybugDB."""
    return [{"error": "Graph analytics not available with LadybugDB"}]


async def run_pagerank(label: str = "*") -> list[dict]:
    """PageRank is not available in LadybugDB."""
    return [{"error": "Graph analytics not available with LadybugDB"}]


async def run_similarity(label: str = "*") -> list[dict]:
    """Similarity search is not available in LadybugDB."""
    return [{"error": "Graph analytics not available with LadybugDB"}]
{% endraw %}
